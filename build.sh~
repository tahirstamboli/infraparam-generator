#!/bin/sh

home=$(pwd) &&
cd ../platform-utils &&
mvn clean install &&
cd $home &&
mvn clean &&
mvn clean compile assembly:single &&
mv target/encryption-utils-0.0.1-jar-with-dependencies.jar $home/encrypt-decrypt-utils.jar
